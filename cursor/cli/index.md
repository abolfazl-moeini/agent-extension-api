# Cursor CLI | Cursor Docs

Source: https://cursor.com/docs/cli/overview

# Cursor CLI

Cursor CLI lets you interact with AI agents directly from your terminal to write, review, and modify code.

## Getting started

```bash
# macOS, Linux, WSL
curl https://cursor.com/install -fsS | bash

agent
```

## Interactive mode

`agent` or `agent "your prompt"`

## Modes

Agent (default), Plan (Shift+Tab / --plan), Ask (--mode=ask)

## Non-interactive / automation

`agent -p "prompt" --model "..."`

## Cloud handoff, sessions, sandbox, Max mode, sudo prompting

Full details and flags in source page.

See also using-agent, acp, reference docs.

---

## Sitemap

[Overview of all docs pages](/llms.txt)
